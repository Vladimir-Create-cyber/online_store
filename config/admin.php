<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Admin Route Prefix
    |--------------------------------------------------------------------------
    |
    | Префикс для URL-адресов административной панели
    | Пример: /admin, /backend, /control
    |
    */
    'prefix' => env('ADMIN_PREFIX', 'admin'),

    /*
    |--------------------------------------------------------------------------
    | Redirect After Login
    |--------------------------------------------------------------------------
    |
    | Куда перенаправлять администратора после успешного входа
    |
    */
    'redirect_after_login' => env('ADMIN_REDIRECT_AFTER_LOGIN', '/dashboard'),

    /*
    |--------------------------------------------------------------------------
    | Admin Guard
    |--------------------------------------------------------------------------
    |
    | Название guard для аутентификации администраторов
    |
    */
    'guard' => 'admin',

    /*
    |--------------------------------------------------------------------------
    | Password Reset
    |--------------------------------------------------------------------------
    |
    | Настройки сброса пароля для администраторов
    |
    */
    'passwords' => [
        'expire' => 60, // Время жизни токена в минутах
        'throttle' => 60, // Задержка между запросами
    ],
];
